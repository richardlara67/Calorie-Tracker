<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.CaloriesCounter.caloriecounter">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calorie_counter_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WeightGoalActivity"
            android:label="@string/title_activity_weight_goal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ExerciseLevelActivity"
            android:label="@string/title_activity_exercise_level"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProfileResultsActivity"
            android:label="@string/title_activity_profile_results"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Track"
            android:label="@string/title_activity_track"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>